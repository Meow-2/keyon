; mine-key 按键映射配置
; 当前实际启用状态以文件底部未注释的配置为准。
;
; 使用方法：
;   1. 复制下面的示例。
;   2. 去掉每行开头的分号。
;   3. 按需要修改 hotkey 和 sendKeys。
;   4. 完整字段说明见 README.md 的“按键映射配置”章节。
;
; [keyMap.esc1]
; enabled=true
; hotkey=Esc & 1
; sendKeys=#1
